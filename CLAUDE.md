# Slip — Claude Instructions

## Commits
Suggest committing whenever a meaningful chunk of work is complete — deployed changes, new features, prompt updates, script additions, or document updates. Ryan forgets to commit, so proactively recommend it with a suggested message.

## Changelog
Always update `CHANGELOG.md` under `[Unreleased]` when making changes. Cut a new version when deploying.

## Versioning
Patch (x.x.1) for prompt tweaks, bug fixes, and script updates. Minor (x.1.0) for new features. Major (1.0.0) for breaking changes.
